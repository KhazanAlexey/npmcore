{
  "name": "library5",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "library5/src",
  "projectType": "library",
  "tags": ["library5"],
  "targets": {
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["library5/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/library5"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["coverage/library5"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../coverage/library5"
      }
    }
  }
}
